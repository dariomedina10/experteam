@model IEnumerable<Experteam.pago>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Crear Nuevo Pago", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TipoPago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TipoPago)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.IdPago }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.IdPago }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.IdPago })
        </td>
    </tr>
}

</table>
